# Session Handover – 2025-07-31

## Last Achieved
- **Refactor Planning Finalized**:
  - Designed step-by-step plan to handle new Textract flow (separate FORMS and TABLES jobs).
  - Defined new unified payload structure including `bill_owner`, `product_id`, `bill_date`, and page-level `currency_markers`.
  - Decided to keep normalized expense output with derived `bill_id` and placeholder `user_owner`.
  - Chose to retain extracted payloads (not raw Textract JSON) for 90 days, with automated cleanup.

- **Retention & Audit Decisions**:
  - Created `data_retention.md` documenting retention for:
    - Raw Textract JSON → not stored.
    - Unified extracted payloads → kept for 90 days.
    - Uploaded bill PDFs → always stored.
    - Database → no automatic deletion.
  - Defined `audit/` folder purpose for pipeline runs vs. `data/` (static reference) and `tests/fixtures/` (test data).

- **Testing Module Progress**:
  - Reviewed and adapted `test_upload.py` to:
    - Test duplicate file handling with CLI prompt (`ignore` vs `replace`).
    - Upload files under `test_uploads/` S3 prefix for safe integration tests.
    - Retain manual runner for one-off debugging.
  - Discussed integration vs unit testing:
    - Decided to keep AWS S3 integration style for now.
    - Mock-based tests deferred.

- **Environment & IDE Path Issues**:
  - Diagnosed why `sys.path.append()` was used.
  - Confirmed `__init__.py` already present.
  - Explained standard industry solutions:
    - Running tests from project root.
    - Configuring `pytest.ini` and VS Code `extraPaths`.
    - Optional editable install (`pip install -e .`).

---

## Current Open Issues
- **Import Path Setup**:
  - Need to finalize IDE configuration to remove `sys.path.append()` hacks.
  - Decide on either:
    - Using `pytest.ini` with `pythonpath = .`
    - Editable install with `setup.cfg`.

- **upload_to_s3.py**:
  - Future change: move duplicate prompt to WhatsApp (once AWS Lambda and UI integration are ready).

- **Retention Policy Implementation**:
  - Middleware cleanup service for 90-day extracted payload retention is pending implementation.

- **Testing Scope**:
  - Duplicate tests adapted but duplicate prompt handling is CLI-based (interactive).
  - Future non-interactive test flow needed once WhatsApp UI is used.

---

## Next Steps
1. Finalize environment setup:
   - Add `pytest.ini` and configure VS Code (`extraPaths`).
   - Remove `sys.path.append()` from tests.
2. Complete Step 2 of refactor:
   - Implement `trigger_textract.py` dual job call.
3. Build `parse_textract_output.py` refactor using new template instructions.
4. Adapt `extract_expenses.py` to new unified payload structure.
5. Update ground truth and adjust validation tests for metadata + currency markers.
6. Implement audit payload cleanup service (90-day retention).

---

## Pending Decisions
- Should we introduce editable install (`pip install -e .`) now or wait until refactor is complete?
- Should we store audit payloads locally during development or directly in S3 (with 90-day lifecycle)?
- When to move duplicate handling from CLI to WhatsApp prompt (Phase 2 or post-MVP)?